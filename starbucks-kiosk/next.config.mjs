/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/refactoring-portfolio-next/starbucks-kiosk',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
